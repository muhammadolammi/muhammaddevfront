

import React from 'react'
import { Playlist } from '../models'

type Props = {
    playlists : Playlist[]
}

const PlaylistBar = (props: Props) => {
  return (
    <div className="playlistBar">
           
    </div>
  )
}

export default PlaylistBar